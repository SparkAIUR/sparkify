<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 520 120" role="img" aria-label="{{NAME}} logo">
  <defs>
    <linearGradient id="logo-bg" x1="0" y1="0" x2="1" y2="1">
      <stop offset="0%" stop-color="{{BG}}" />
      <stop offset="100%" stop-color="{{ACCENT}}" />
    </linearGradient>
  </defs>
  <rect x="6" y="16" width="88" height="88" rx="20" fill="url(#logo-bg)" />
  <circle cx="50" cy="60" r="28" fill="none" stroke="{{FG}}" stroke-width="3" opacity="0.85" />
  <text x="50" y="66" text-anchor="middle" font-size="16" font-family="ui-sans-serif, system-ui, -apple-system, Segoe UI, sans-serif" font-weight="800" fill="{{FG}}">{{MARK}}</text>
  <text x="116" y="62" font-size="32" font-family="ui-sans-serif, system-ui, -apple-system, Segoe UI, sans-serif" font-weight="700" fill="{{BG}}">{{NAME}}</text>
  <rect x="116" y="72" width="290" height="6" rx="3" fill="{{ACCENT}}" opacity="0.9" />
</svg>
