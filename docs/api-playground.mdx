---
title: "API Playground"
description: "Interactive OpenAPI reference and Try It behavior in static docs."
---

# API Playground

`sparkify` v0.1.0 uses Stoplight Elements for interactive API pages.

## Support

- API key auth
- Bearer auth
- Basic auth
- OAuth2 browser-safe flows:
  - Authorization Code + PKCE
  - Device Code

## Static hosting constraints

On GitHub Pages, Try It requests are browser-side:

- API must allow CORS, or
- configure `playground.tryItCorsProxy`.

## Config example

```json
{
  "playground": {
    "provider": "stoplight",
    "tryItCorsProxy": ""
  }
}
```
